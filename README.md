1. Create venv bycommand :-    python -m venv venv_name
2. activate virtual  by venv_name/Scripts/activate
3. install : - pip install fastapi uvicorn pydantic
4. for run use program.py
5. URL is 'http://localhost:8000/api/endpoint/12'